<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.PawsConnect" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PawsConnect" parent="Base.Theme.PawsConnect" />

    <style name="AppSearchView" parent="Widget.AppCompat.SearchView">
        <item name="android:editTextColor">@color/additional</item>
        <item name="android:textColor">@color/main</item>
        <item name="android:textColorHint">@color/additional</item>
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">@font/nunito_regular</item>
    </style>
</resources>